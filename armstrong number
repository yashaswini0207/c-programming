#include<stdio.h>
void main ()
{
int n,r,num,s=0;
printf("enter the number");
scanf("%d",&n);
num=n;
while(n>0)
{
 r=n%10;
 s+=r*r*r;
 n=n/10;
}
printf("sum of cubes of digits %d",s);
{
if(num==s)
printf("%d is a armstrong number",num);
else
printf("%d is not a armstrong number",num);
}
} 
