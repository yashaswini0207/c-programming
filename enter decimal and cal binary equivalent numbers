#include <stdio.h>
void main()
{
int n,a[20],i;
printf("enter number:");
scanf("%d",&n);
for(i=0;n>0;i++)
{
a[i]=n%2;
n=n/2;
}
printf("\n binary number of the given numbeer is");
for(i=i-1;i>=0;i--)
{
    printf("%d",a[i]);
}
}
